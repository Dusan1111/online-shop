// import { LogSignInComponent } from 'src/app/log-sign-in/log-sign-in.component';
import { LogSignInComponent } from 'src/app/log-sign-in/log-sign-in.component';

describe('LogSignInComponent', () => {
    let fixture: any;
    let sharedServiceMock: any;
    let alertServiceMock: any;
    let routerMock: any;
    let formGroupMock: any;

    // fixture = new LogSignInComponent(sharedServiceMock, alertServiceMock);

    // test('login unsuccessful', () => {
    //     fixture.logIn();
    //     expect(fixture.loginSuccessful).toBeTruthy();
    // });
    
});

